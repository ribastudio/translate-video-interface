// instance elements
const btnPlay = document.getElementById('play-btn');
const screen = document.getElementById('editor-modal');
const modalBack = document.querySelector('.modal');

window.addEventListener('click', (event) => {
  if (event.target === modalBack) {
    
  }
})
